<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorWhite"
    tools:context="edu.campusvirtual.comunica.activities.LoginActivity">

    <ImageView
        android:id="@+id/headerImageViewId"
        android:layout_width="0dp"
        android:layout_height="198dp"
        android:contentDescription="imageView"
        android:background="@color/colorPrimary"
        app:layout_constraintBottom_toTopOf="@+id/scrollView2"
        app:layout_constraintEnd_toEndOf="parent"
        android:scaleType="fitXY"
        android:adjustViewBounds="true"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ScrollView
        android:id="@+id/scrollView2"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/headerImageViewId">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:orientation="vertical">
            <EditText
                android:id="@+id/usernameTextEditId"
                android:layout_width="314dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:layout_marginStart="8dp"
                android:layout_marginTop="25dp"
                android:background="@drawable/bg_edit_text"
                android:drawableStart="@android:drawable/sym_action_email"
                android:drawablePadding="15dp"
                android:hint="username"
                android:layout_gravity="center"
                android:textSize="13dp"
                android:inputType="textEmailAddress"
                android:padding="5dp"
                android:paddingStart="10dp"
                tools:ignore="RtlSymmetry" />

            <EditText
                android:id="@+id/passwordTextEditId"
                android:layout_width="314dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:layout_marginStart="8dp"
                android:layout_marginTop="10dp"
                android:textSize="13dp"
                android:background="@drawable/bg_edit_text"
                android:drawablePadding="15dp"
                android:drawableStart="@android:drawable/ic_lock_idle_lock"
                android:hint="password"
                android:inputType="textPassword"
                android:padding="5dp"
                android:paddingStart="10dp"
                android:layout_gravity="center"
                tools:ignore="RtlSymmetry" />

            <Button
                android:id="@+id/buttonLoginId"
                android:layout_width="314dp"
                android:background="@drawable/bg_rounded"
                android:textColor="@color/colorWhite"
                android:layout_marginTop="20dp"
                android:textSize="16dp"
                android:layout_gravity="center"
                android:layout_height="40dp"
                android:text="Ingresar" />
            <Button
                android:id="@+id/buttonLoginGoogleId"
                android:layout_width="314dp"
                android:background="@drawable/bg_rounded_google"
                android:textColor="@color/colorWhite"
                android:layout_marginTop="10dp"
                android:textSize="14dp"
                android:layout_gravity="center"
                android:layout_height="40dp"
                android:text="Inicia sesión con Google" />
            <Button
                android:id="@+id/buttonLoginFacebookId"
                android:layout_width="314dp"
                android:background="@drawable/bg_rounded_facebook"
                android:textColor="@color/colorWhite"
                android:layout_marginTop="10dp"
                android:textSize="14dp"
                android:layout_gravity="center"
                android:layout_height="40dp"
                android:text="Inicia sesión con Facebook" />
        </LinearLayout>
    </ScrollView>


</androidx.constraintlayout.widget.ConstraintLayout>
